# Archive

Previously contained the vendored truck B-Rep library (geometry kernel through
Sprint 67). Removed to reduce repo size. See git history for reference.

The active kernel is `crates/kernel/`.
