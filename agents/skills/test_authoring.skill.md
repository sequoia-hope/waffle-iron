# Skill: Test Authoring (v1)

Purpose:
Create failing, invariant-driven tests.

Requirements:

- At least one test per branch.
- Canonical predictable case.
- Edge case.
- Numeric or structural assertions required.

Tests must fail on current implementation.

